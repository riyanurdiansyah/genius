@{
    ViewData["Title"] = "Container";
}

<div class="card">
    <div class="card-body text-center py-5">
        <i class="fas fa-columns fa-3x text-muted mb-3"></i>
        <h4>Container Layout</h4>
        <p class="text-muted">This is an empty page placeholder for the Container layout.</p>
    </div>
</div>
