@{
    ViewData["Title"] = "Fluid";
}

<div class="card">
    <div class="card-body text-center py-5">
        <i class="fas fa-columns fa-3x text-muted mb-3"></i>
        <h4>Fluid Layout</h4>
        <p class="text-muted">This is an empty page placeholder for the Fluid layout.</p>
    </div>
</div>
